# -----------------------------
# Core scientific stack
# -----------------------------
numpy>=1.23
scipy>=1.9
pillow>=9.5
opencv-python>=4.8

# -----------------------------
# PyTorch (install separately with correct CUDA!)
# -----------------------------
# DO NOT install torch here to avoid CUDA mismatches.
# Install manually, e.g.:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch
# torchvision>=0.15

# -----------------------------
# Segment Anything (SAM)
# -----------------------------
git+https://github.com/facebookresearch/segment-anything.git

# -----------------------------
# LLM backends (optional, choose what you need)
# -----------------------------

# OpenAI (GPT-4V / GPT-4.1 / GPT-4o)
openai>=1.3

# Anthropic (Claude 3.5 Vision)
anthropic>=0.25

# Google Gemini Vision
google-generativeai>=0.4

# -----------------------------
# Utilities
# -----------------------------
tqdm>=4.66
requests>=2.31
