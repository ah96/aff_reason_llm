# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
*.so
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Model checkpoints (large binary files)
*.pth
*.pt
*.ckpt
*.bin
*.safetensors
ooal_models*/
sam_vit_*.pth

# Experiment outputs (data, not code)
out_b/
outputs/
cache_b/
experiment_b_bundle/out/

# API keys / secrets
.env
*.env
secrets.json

# Claude Code
.claude/

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.toc

# Datasets (too large for git)
data/
datasets/
ade_affordance/

# Paper — kept on Overleaf, not GitHub
overleaf/

# Reference material / PDFs (large, not needed to run anything)
related_work/
*.pdf

# Experiment A data + cache + raw predictions (archived separately).
# Keeps the small summary *.json + score_from_raw.py + results/README.md trackable.
experiment_a_bundle/
experiment_a/cache_a_vision/
experiment_a/results/*.jsonl

# Experiment B — SAM checkpoints (auto-downloaded) + archived dead code
experiment_b/checkpoints/
experiment_b/legacy/
# NOTE: experiment_b_bundle/images/ is intentionally NOT ignored — the 200 scenes
# are committed so the lab PC gets them on `git pull`.
